import 'package:bloggingapp/imports/custom_import.dart';

class Height10 extends StatelessWidget {
  const Height10({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 0.01.sh,
    );
  }
}

class Height20 extends StatelessWidget {
  const Height20({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 0.02.sh,
    );
  }
}

class Height40 extends StatelessWidget {
  const Height40({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 0.04.sh,
    );
  }
}
